<!doctype html>
<html class="no-js" lang="en-ca">

<head>
	<meta charset="utf-8">
	<title>{% if page.url !== '/' %}{{ title }} | {% endif %}Everywhere School</title>
	<meta name="description" content="{{ site.description }}">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="/normalize.css">
	<link rel="stylesheet" href="/style.css">
	<link rel="shortcut icon" href="/images/favicon.ico">
</head>

<body>
    <div class="container">
        <header role="banner" class="card card--title">
            <a href="/">
                <img src="/images/logo.svg" alt="" />
                <span class="sr-only">Everywhere School</span>
            </a>
            {% set brandTag = 'h1' %}
            {% if page.url !== '/' %}
            {% set brandTag = 'p' %}
            {% endif %}
            <{{ brandTag }} class="brand">Everywhere School</{{ brandTag }}>
        </header>
        <div class="card card--title">
            <p class="subhead">A calendar of educational livestreams for kids during the COVID-19 pandemic</P>
        </div>
        <div class="card card--title">
            {% include 'partials/navigation.njk' %}
        </div>
        
        <main>
        {% block content %}
        {% endblock content %}
        </main>
    </div>
    {% include 'partials/footer.njk' %}
    <script type="module" src="/app.js"></script>
    <script type="nomodule" src="/web_modules/app.nomodule.js"></script>
</body>

</html>
